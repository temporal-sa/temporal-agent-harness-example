terraform {
  backend "s3" {
    bucket       = "replace-with-terraform-state-bucket-name"
    key          = "temporal-agent-harness-example/bootstrap.tfstate"
    region       = "us-west-1"
    encrypt      = true
    use_lockfile = true
  }
}
